import { i18n, arg } from '@site/src/components/I18n';

export default i18n()
  //
  .trans(['状态'])
  .en('Status')
  //
  .trans(['开始时间'])
  .en('Start Date')
  //
  .trans(['结束时间'])
  .en('End Date')
  //
  .trans(['开发内容'])
  .en('Content')
  //
  .trans(['备注'])
  .en('Comment')
  //
  .trans(['已完成'])
  .en('Done')
  //
  .trans(['进行中'])
  .en('Doing')
  //
  .trans(['未开始'])
  .en('Pending')
  //
  .trans(['已取消'])
  .en('Discarded')
  //
  .done();
